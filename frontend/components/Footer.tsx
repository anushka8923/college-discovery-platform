export function Footer() {
  return (
    <footer className="border-t border-black/5 bg-white">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-6 text-sm text-ink/65 sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">
        <p>College Discovery Platform MVP</p>
        <p>Search, compare, and predict with real API-backed data.</p>
      </div>
    </footer>
  );
}
